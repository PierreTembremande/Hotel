{% extends 'base.html.twig' %}

{% block title %}Hello ReservationController!{% endblock %}

{% block body %}
<style>

</style>

<div class="example-wrapper">

	<h1>Listes des réservations de l'hotel</h1>

	<table class="table table-dark table-hover">

		<thead>

            <th>date de reservation</th>
			<th>date arrivee</th>
			<th>duree</th>		
			<th>chambre</th>
			<th>client</th>

		</thead>

		<tbody>

			{% for reservation in reservations%}

                {% if reservation.dateArrivee|date("d/m/Y H:i:s") > "now"|date("m/d/Y") %}


                    <tr>
                        <td>{{reservation.dateResa|date("d/m/Y")}}</td>
                        <td>{{reservation.dateArrivee|date("d/m/Y H:i:s")}}</td>
                        <td>{{reservation.duree}}</td>
                        <td>{{reservation.chambre.numero}}</td>
                        <td>{{reservation.client.Nom}}{{reservation.client.Prenom}} </td>
                    </tr>

                {% endif %}

			{% endfor %}

		</tbody>

	</table>

{% endblock %}
